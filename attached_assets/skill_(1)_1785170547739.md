---
name: replit-enterprise-api
description: Call the Replit Enterprise API (api.replit.com/v1). Use when building or changing anything that reads Enterprise account data — usage/cost dashboards, spend reports, member or project directories.
---

# Replit Enterprise API

Base URL `https://api.replit.com/v1`. Auth: `Authorization: Bearer <key>` — keep the key in an environment variable (e.g. `REPLIT_ENTERPRISE_API_KEY`), never in code. To get a key, the user goes to **Replit account Settings → Developer tab**; API keys there are only available to **account admins**, so a non-admin must ask an admin of their Enterprise account to generate one. All endpoints are read-only, scoped to the key's account. Full contract: fetch `https://api.replit.com/openapi.json` — consult it when designing any endpoint or type.

| Endpoint | Purpose | Rate limit | Pagination |
|----------|---------|------------|------------|
| `GET /usage` | Cost/usage; `groupBy` = `member`\|`project`\|`workspace`, `granularity=day`, `userId`/`projectId` filters | **~100 req/min** | cursor, `pageSize` ≤ 100 |
| `GET /members` | Member directory (role, lastSeen, workspaces) | ~10k/min | cursor |
| `GET /workspaces` | Workspace directory | ~10k/min | cursor |
| `GET /projects` | Projects; `hasDeployment=true` filter | ~10k/min | cursor |

## The rate budget

`/usage` grants ~100 requests/minute; every design decision flows from that budget.

- Route **every** `/usage` call through one **serial queue** with priorities (high = interactive request, low = background build). The queue paces via `X-RateLimit-Remaining` / `X-RateLimit-Reset` headers and retries 429s honoring `Retry-After`. Concurrent usage calls are the one hard prohibition: they burn the shared budget and starve interactive requests.
- Do the budget math before designing: one call per entity means minutes of wall time (~70 entities ≈ 7 min). Per-entity features must build **progressively**: background low-priority fetches, partial responses carrying `isComplete` + pending count, client polling (~8s) until complete.
- Directory endpoints are effectively free — paginate them fully (thousands of projects ≈ tens of calls is fine).

## Usage data semantics

Grouped results return `groups[]`: `key` (userId/projectId/workspaceId/date), `totalCostUsd`, and `metrics[]` — the line items (`metricType` e.g. `BILLABLE_METRIC_AI_AGENT`; `category` = `ai`, `project_hosting`, `storage`, `project_traffic_and_data`; `usage` nullable, `usageUnit` nullable, `costUsd` always present).

- Groups arrive **ordered by descending `totalCostUsd`**, so capped pagination always keeps the top spenders.
- Capped pagination creates a **blind spot**: on a ~9k-project account, 5 pages (500 groups) hid everything under $20; 20 pages (2000 groups) shrank the blind spot to under $1. When a feature claims something has *zero* cost, fetch deep enough, compute the **capture threshold** (smallest positive cost fetched), return it from your API, and disclose it in the UI ("costs below $X may not be captured"). A zero-cost claim is complete only when the threshold is exposed alongside it.
- Zero-spend members are absent from `groupBy=member` results — join against `/members` to include them.
- Per-entity daily series cost one `/usage` call each (entity filter + `granularity=day`); there is no bulk endpoint.
- Some group projectIds refer to deleted projects and miss on join against `/projects` — treat joins as optional.
- Cursor pagination: pass `pageToken` from the prior response, stop when absent. Cap `maxPages` and **log a warning on truncation** — the blind spot must be visible in logs, since it is invisible in the data.

## Caching

Fresh data costs minutes of rate-limited calls, so every interactive app needs a cache. Two tiers:

**In-memory** — the default for a single-server app. Cache promises keyed by `endpoint:range`; usage TTL ~10 min, directory ~15 min. **Warm up** the default range at server startup so first loads are instant. Restarts start cold, and repopulation is rate-limit bound (minutes) — acceptable when the app tolerates a progressive fill after restart.

**Database-backed** — choose when restarts/redeploys must keep warm data. Persist the cache in any SQL database:

- Schema: `api_cache(key text primary key, payload jsonb, fetched_at timestamptz)` with the same `endpoint:groupBy:days` keys; plus a per-entity daily table (`entity_id, date, cost_usd, fetched_at`) so a partially-built daily matrix survives restarts and **resumes** — on boot, load persisted rows and fetch only missing or stale entities.
- Read path: serve from DB within TTL; refresh stale entries in the background (stale-while-revalidate) so requests stay fast.
- Write path: upstream fetches still go through the serial queue — the DB is a cache layer under the same rate budget.
- If dev and production use separate databases, production boots cold — keep the warm-up logic.

## Proven architecture

A pattern validated in production against a large account (~175 members, ~9k projects): a thin backend proxy in front of the Enterprise API (browser clients can't call it directly — the key must stay server-side), an OpenAPI contract for your own `/api`, generated typed client hooks, the serial usage queue, in-memory caches with startup warm-up, and a progressive background builder for per-member daily series with client polling until `isComplete`.