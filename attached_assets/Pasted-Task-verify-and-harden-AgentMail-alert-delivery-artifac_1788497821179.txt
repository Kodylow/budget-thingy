Task: verify and harden AgentMail alert delivery. artifacts/api-server/src/lib/email.ts is already implemented against the Replit AgentMail connector. Do not rewrite it. Do not touch enterprise.ts, ingest.ts, or usage schema.

Prove the connector works from the server. Run isEmailConfigured() in the dev workspace and paste the result plus the log line. Then call sendEmail([<Kody's bootstrap email>], "Budget Monitor test", "<p>test</p>") from a one-off script and paste the returned SendResult (messageId, senderEmail). Report the sender address AgentMail assigned to the group-budget-monitor inbox.
Add a test-send endpoint. POST /api/admin/email/test, requireTrueAccountAdmin. Sends buildAlertEmail(...) with sample values (group "Example Group", 90% threshold, $900 of $1,000) to the caller's own verified email only, ignoring the recipients list. Returns the SendResult. Add it to lib/api-spec/openapi.yaml and regenerate the client. Add a "Send test email" button on the Settings → System status card next to the existing emailConfigured indicator, showing senderEmail and messageId on success or the error text on failure.
Make readiness cheap. isEmailConfigured() currently lists inboxes over the network on every call; /status and every checker run pay that. Cache the resolved sender inbox in memory for 10 minutes on success; keep re-probing every call while it fails. Keep creating a fresh ReplitConnectors per send (connector credentials are short-lived).
Alert email content. Add to buildAlertEmail: the reporting window label already passed, plus dataAsOf (new arg, formatted UTC) and a link to the app's group or team page (APP_BASE_URL env var; omit the link if unset). Keep the existing subject format.
Deliverability check. After step 1, report whether the test landed in Kody's inbox or spam. If spam: note it in replit.md under Gotchas; the fix is a custom sending domain on the AgentMail side, which Kody configures, not code.
Update replit.md: replace "awaiting an email connector" with one line describing the AgentMail connector, the dev-only redirect to the bootstrap address, and the [DEV] prefix.
Commit: "Verify AgentMail delivery and add admin test-send".
Report back: the SendResult from step 1, the sender address, inbox vs spam, and a screenshot of the Settings card after a successful test send.

Two things to decide on your side, not the agent's:

Sender identity. The auto-created inbox will be something like group-budget-monitor-xxxx@agentmail.to. Comcast's mail filters will likely treat that as unknown-sender. If these alerts need to reach Comcast finance people reliably, set up a custom domain in AgentMail (their dashboard supports it) before cutover. If the recipients are just you and one or two Replit people for now, skip it.
Recipients list. The recipients table is empty in production as far as the dump showed (it wasn't queried). Once Step 3 makes the checker real, decide who goes on it; until then the checker will keep skipping with "no admin emails configured".