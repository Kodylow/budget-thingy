What's right: ingest.ts is 647 readable lines and does what the brief said. Fetch happens fully in memory before the transaction, so #203 is fixed and a failure leaves prior rows intact (ingest.ts:250-316). Rate budget is now a fixed-minute bucket at 170/min for /usage, 600/min total, with a 429 embargo. rawFetch refuses any call outside the ingest context, so nothing else can touch /usage by accident. Reconciliation writes to ingest_reconciliation and marks drifted days for refetch. replit-budgets.ts uses its own transport, so the usage-limit write feature is unaffected by the gate.

Three defects to fix (all small):

history.ts's roster snapshot job calls getDirectory(true) → rawFetch → now throws "restricted to the usage ingestion scheduler". Daily roster snapshots are broken as of this commit. Fix: run the roster snapshot from inside the ingest cycle (after refreshMetadata, once per UTC day), delete startSnapshotJob.
reconcile() compares the current month's upstream total including today against rows fetched minutes earlier. Any usage arriving in between exceeds $0.01, marks the whole current month failed, and refetches it every day. Cap the current-month comparison at today − 3 so it only checks final days.
refreshMetadata waits on the legacy queue (while (pendingUsageCount() > 0)) with no timeout. If that queue ever stalls, ingestion stalls with it. Bound it (60 s) and log; Step 5 deletes the queue entirely.
Step 0's deployment items were not done: .replit still says autoscale, post-merge.sh still runs push-force. Folded in below.

Next task, paste as-is:

Task: Step 2 — read path from Postgres. Read docs-free; the contract is below. Do not modify ingest.ts except items 1–3.

Before the read path (small fixes):

Move the daily roster snapshot into the ingest cycle: after refreshMetadata(), if no group_roster_snapshot_days row exists for today, capture it from the directory just refreshed. Delete startSnapshotJob and its interval.
In reconcile(), for the current month compare only through today − 3 (effectiveEnd = min(monthEnd, today − 2) exclusive). Closed months unchanged.
In refreshMetadata(), bound the pendingUsageCount() wait to 60 s; on timeout log a warning and continue.
.replit: deploymentTarget = "vm". scripts/post-merge.sh: replace push-force with pnpm --filter @workspace/db run migrate applying lib/db/drizzle/*.sql.
New artifacts/api-server/src/lib/usage-store.ts:

interface UsageWindow { start: string; end: string }  // YYYY-MM-DD UTC, end exclusive
interface UsageSnapshot {
  window: UsageWindow;
  memberByWorkspace: Map<string, Map<string, { totalUsd: number; aiUsd: number }>>;
  projectByWorkspace: Map<string, Map<string, number>>;
  workspaceTotals: Map<string, { totalUsd: number; memberAttributableUsd: number; memberUnattributableUsd: number }>;
  dailyByWorkspace: Map<string, Map<string, number>>;
  dailyMemberByWorkspace?: Map<string, Map<string, Map<string, number>>>;  // only with perDay
  accountTotalUsd: number | null;
  coverage: { requiredWorkspaceDays: number; presentWorkspaceDays: number; failedWorkspaceDays: number; oldestFetchedAt: string | null; newestFetchedAt: string | null };
  status: "complete" | "stale" | "partial" | "empty";
  dataAsOf: string | null;
}
getUsageSnapshot(window, opts?: { workspaceIds?: ReadonlySet<string>; perDay?: boolean }): Promise<UsageSnapshot>
invalidateUsageStore(): void   // called by ingest after every successful unit commit
Four SUM … GROUP BY queries over the four usage_*_day tables, usage_date >= start AND usage_date < end, workspace_id = ANY($1) when scoped. status: empty if zero workspace-day rows; partial if any status='failed' in the window; stale if the window includes today and newestFetchedAt is older than 20 min; else complete. In-memory memo keyed by (window, workspaceIds, perDay), cleared only by invalidateUsageStore(). No other usage cache may exist.

Window resolution: replace resolveRange with pure resolveWindow(rangeType, startDate?, endDate?, now). billing uses the persisted billing-period anchor; full-term starts at the cutoff; MTD/YTD from month/year start; all end at tomorrow 00:00Z. Delete UsageRange.key, rangeKey, and every range-keyed cache path.

Rollup: keep computeDedupedUsageRollup and the creator-attribution logic as pure functions in a new rollup.ts. Write buildRollupInputs(snapshot, directory, projectInfo) that derives their inputs: group member usage = workspace member rows filtered by directory.groupMembers; project attribution = projectByWorkspace joined to projectInfo.creatorId; ModelFarm and hosting metrics follow the project's creator like all non-Agent cost. Remove the materializedRollup / canonicalMonthly* branch from getCanonicalUsage and move it into rollup.ts with the snapshot as its only usage input.

Historical attribution: for /trends and any window ending before today, attribute each day using that day's group_roster_snapshots row (attributeHistoricalDay), falling back to the current directory when a day has no snapshot. Use perDay: true.

Routes to rewire in routes/monitor.ts: /groups, /groups/:groupId, /groups/:groupId/projects, /clusters/:clusterKey/headline, /clusters/:clusterKey/projects, /summary, /teams/budgets, /trends, /export/users.csv, /projects/export, /users/activity, /alerts (current-usage columns), /status.

Response contract: remove isComplete, pendingCount, failedCount, partialCount, projectSyncStatus, projectPendingCount, projectFailedCount, projectPartialCount, spendLoaded, rawMemberSpendLoaded, paceSpendLoaded, rollupSpendLoaded, projectSpendLoaded, usageStale. Add usage: { status, dataAsOf, coverage, unreconciledUsd } where unreconciledUsd = accountTotalUsd − SUM(workspaceTotals). Spend fields are numbers, never null.
Delete POST /groups/:groupId/refresh, POST /usage/retry, POST /usage/ranges/rebuild. Add POST /admin/ingest/run (true admin; runs one runCycle(), returns the CycleSummary) and GET /admin/ingest/runs?limit=20.
/status: last 5 ingest_run rows, remainingBackfillCount, current-month ingest_reconciliation rows, directory age, rate-limit snapshot, emailConfigured. Remove usageSync and accountTotalVerification.
Update lib/api-spec/openapi.yaml first and regenerate the client. Frontend may break on removed fields; that is Step 4. Do not touch the frontend in this step beyond keeping typecheck green (stub with ?? 0/?? true where needed).
Acceptance (report all):

SELECT workspace_id, ROUND(SUM(total_cost_usd)::numeric,2) FROM usage_workspace_day WHERE usage_date BETWEEN '2026-08-19' AND '2026-09-03' GROUP BY 1 ORDER BY 2 DESC; — paste the table.
/summary?rangeType=billing per-workspace totals equal that table.
20 sequential GET /groups?rangeType=full-term: p95 under 300 ms warm, first request under 1 s.
rg -c "queue|rangeKey|getUsageSyncSummary" artifacts/api-server/src/routes/ returns 0.
pnpm typecheck green; run pnpm --filter @workspace/api-server test and report pass/fail counts.