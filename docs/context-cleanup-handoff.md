# Context cleanup handoff

Historical record of the documentation-only cleanup on 2026-09-08. This is not
standing agent guidance, a new product specification, or a runtime acceptance
report.

## Reduction

Counts are whitespace-delimited words in the inspected files, not tokens or a
measurement of automatically loaded context. The before snapshot was taken
before editing, including pre-existing concurrent changes to `replit.md`.

| Inspected set | Before | After |
| --- | ---: | ---: |
| Memory topics and index | 8,346 | 1,109 |
| `replit.md` | 2,258 | 792 |
| Nine targeted optional skill definitions | 28,197 | 25,180 |
| Three duplicate completed-plan copies | 2,982 | 0 |
| Four handoff/guide documents | 1,769 | 1,895 |
| **Total compared files** | **43,552** | **28,976** |

Memory went from 26 topics plus index to 7 topics plus index (86.7% fewer words).
Memory and project guidance together dropped 82.1%. The compared set dropped
33.5%; historical labels add a small amount of text. This new handoff is excluded
from those comparisons. Specialized skill reference material was largely
retained rather than rewriting the libraries.

## Removed and consolidated

- Removed the old browser failure count and mandatory stash comparison, mockup
  registration/port workaround, unrelated logo advice, and standing commands to
  port a particular uploaded implementation or preserve an assumed live Airtable
  workflow.
- Removed endpoint inventories, code-generator recipes, provider/hot-update
  debugging recipes, implementation/test descriptions, and duplicated accounting,
  access, projection, and screenshot-privacy instructions from memory.
- Consolidated financial interpretation, auth transitions, acquisition recovery,
  financial write intent, snapshot coherence, resource bounds, and encoded query
  identity rationale into seven short topics. No audit log or completion summary
  was added to memory.
- Replaced release-note-like project narration and frozen layout instructions
  with product/run orientation. Removed the environment-specific personal admin
  identity value without touching its actual configuration.

## Retained boundaries and conflict resolution

`replit.md` remains the single project-level home for the existing funding term,
approved allocation source/total and alias, local additions until Airtable is
explicitly configured, annual funding versus monthly enforcement, exact upstream
write targets, operator overrides, scoped capabilities, revocation, controlled
email delivery, private read-only development viewing, and public sample-data
privacy.

These constraints were preserved from the pre-cleanup guidance and task brief;
this cleanup does not claim new confirmation of business terms or live
integration state.

The current task explicitly identifies the user's rejection of ordinary coverage
badges. Read-only inspection of `dashboard-chart.tsx` and
`dashboard-chart.test.tsx` corroborates admin-only coverage notes, known cumulative
values, and missing period values remaining unavailable. The conflicting memory
mandate for visible chart/tooltip qualifiers was removed, not implemented.
Essential value/basis labels, errors, financial uncertainty, and missing-value
gaps remain protected. Missing historical membership evidence is not treated as
proof that observed workspace charges did not occur.

## Optional skill changes

| Local definition | Before → after words | Change |
| --- | ---: | --- |
| `.agents/skills/ponytail` | 1,079 → 1,003 | Explicit/minimal-solution selection; no always-on persistence or silent scope reduction |
| `.agents/skills/minimalist-ui` | 1,089 → 1,171 | Narrow selection and supplied-design precedence |
| `.agents/skills/redesign-existing-projects` | 2,210 → 2,278 | Requested redesign scope only |
| `.agents/skills/design-taste-frontend` | 12,853 → 12,892 | Distinctive art-direction requests, not ordinary frontend work |
| `.local/custom_skills/deslop` | 701 → 721 | No unsolicited post-change refinement |
| `.local/custom_skills/image-to-code-skill` | 5,823 → 5,870 | Supplied references first; generation only as requested, no mandatory image counts |
| `.local/custom_skills/gpt-tasteskill` | 1,137 → 1,133 | Brief-driven choices, no simulated randomness or mandatory GSAP |
| `.local/custom_skills/minimalist-skill` | 1,092 → 56 | Explicit alias to the canonical minimalist skill |
| `.local/custom_skills/redesign-skill` | 2,213 → 56 | Explicit alias to the canonical redesign skill |

All paths above refer to each directory's `SKILL.md`. These are regular local
files, not symlinks. User requirements and supplied designs take precedence over
their stylistic defaults, subject to safety and accessibility.

## Historical evidence

Byte-for-byte comparisons and matching hashes verified the following duplicate
copies before deletion:

| Deleted copy | Preserved canonical plan |
| --- | --- |
| `.local/tasks/current-6.md` | `.local/tasks/bounded-usage-ingestion.md` |
| `.local/tasks/current-7.md` | `.local/tasks/lean-budget-drilldowns.md` |
| `.local/tasks/current-11.md` | `.local/tasks/apply-taste-review-synthesis.md` |

The assignment identifies these as completed plans. Reference search found only
six intentional cleanup-inventory mentions in the active cleanup brief and its
task copy; those plans also name the canonical files. They were left untouched,
not rewritten to erase evidence of the deletion request. No live dependency on
the removed copies was found.

The Airtable, Comcast transfer, and project-intelligence handoffs now identify
historical requests/observations without removing their evidence or safety
boundaries. The walkthrough remains user-facing guidance, not proof of current
deployment status.

## Validation and remaining limitations

- Memory index shape, topic frontmatter, local Markdown links, skill headers, and
  canonical alias targets passed static checks; `git diff --check` passed.
- Hash comparisons against the starting workspace found no runtime or unrelated
  tracked-file changes by this task. All remaining task-plan files and unrelated
  local skills matched their starting hashes. Pre-existing application changes
  were preserved.
- Rebase conflicts were resolved by retaining incoming `main` application,
  contract, test, and generated files byte-for-byte. The cleaned guidance also
  retains the incoming full-team financial comparison rule; final counts above
  include that addition. The resolved Git diff contains documentation only.
- No application/tests/data/configuration/credentials/migrations/deployments or
  recorded task states were edited. No runtime checks, screenshots, live
  integration actions, or workflow restarts were needed for this documentation
  change.
- Platform-provided instructions/skills, security/export governance skills, and
  the private profile were not edited. Their authority is not replaced here.
- The supplied memory index and skill summaries were injected into this session;
  other files were inspected on demand. Editing local definitions cannot
  retroactively change already-injected summaries. Future discovery refresh and
  runtime alias resolution were not verified.
- Existing Git ignore rules exclude local memory, skills, and task files from
  ordinary Git transfer. They were cleaned in place; tracking/export policy was
  not changed. This handoff does not claim those ignored edits travel with a
  normal clone or that all on-disk context is automatically loaded.
- No new follow-up task was proposed: existing tasks already cover the known
  product/browser/production work, and remaining discovery/transfer caveats do
  not authorize additional application or platform changes.